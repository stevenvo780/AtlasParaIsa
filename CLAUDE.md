# Una Carta Para Isa — guía para Claude Code

- Lee `GOAL.md`, `PLAN.md`, `docs/REGLAS.md`, `docs/EVIDENCIA.md` antes de tocar reglas. La constitución del proyecto está en `.specify/memory/constitution.md` y manda.
- Estado publicado (qué commit, qué reglas, qué mundo): **`docs/ESTADO.md`**, la única fuente; README, PLAN y esta guía enlazan a ella en vez de copiarla.
- Revisiones vigentes (leerlas antes de tocar reglas): `docs/REVISION-2026-09-24.md` (C8 v3 auditado: no alcanzable por un mundo sano estacionario; la nueva lectura de C8 la decide Steven; NAT-L refutada en el cribado), `docs/REVISION-2026-09-22.md` y `docs/REVISION-NOCHE-2026-09-22.md` (extinción pública V7, gobernador `techo`, congelación de 002). `docs/REVISION-2026-09-19.md` es histórica.
- Flujo SDD (Spec Kit): `specs/001-mundo-solido-masivo/` quedó cerrada el 2026-09-19 (T001–T049; sus casillas no se mantienen). `specs/002-mundo-ilimitado/` (mundo que crece hasta el límite del hardware, determinista) tiene la Etapa A integrada; las etapas B–F se congelaron el 2026-09-22 (`docs/REVISION-NOCHE-2026-09-22.md`) y **Steven las descongeló el 2026-09-23** para no probar solo mundos de techo bajo: la torre se reparte en ≈ 8 hilos de biología (panel fijo de 8 semillas, un brazo a la vez), ≈ 4 de mundos grandes sin techo de laboratorio y ≈ 16 + GPU de escala. PERF3 y la ola 1 de E.0 (T141–T143) y de la etapa B (T111–T113) ya están integradas en `main`; la ola 2 de B (T115/T117/T120) queda en espera porque el perfil a 700 habitantes (bitácora 23-09 17:50) no la señala como el coste principal (decisión D4 del plan maestro del 23-09). De esa etapa A, en `tasks.md` siguen T103 y T104 con nota de reescritura (superadas por ARCH/PERF3 y por el perfil) y el **Gate A (T110)** abierto; **T100** (topes de anticorrupción derivados de parámetros) sigue abierta. La prioridad es la biología (criterio de 60 días de `GOAL.md`), con diagnóstico barato antes que escala. **Para ejecutar tareas SDD: `/speckit-implement`.** Reparte las tareas `[P]` entre subagentes en paralelo (ficheros disjuntos) y cierra cada fase con `npm run typecheck && npm test` y un barrido de control del laboratorio. Cada tarea de `tasks.md` lleva su modelo y esfuerzo asignado; delegar vía `delegar_a_cloud` con `access=write` y `cwd` del worktree correspondiente.
- **Orden del espacio de trabajo** (Steven, 2026-09-23): junto al repositorio solo existe `/datos/workspaces/personal/AtlasParaIsa-anexo/` con `worktrees/` (todo worktree nuevo va ahí: `git worktree add ../AtlasParaIsa-anexo/worktrees/<nombre> …`), `mundos/` (copias de mundos de la torre, incluida la V10 congelada de la mudanza), `publicaciones/` (respaldos) y `archivo/`. Nunca crear carpetas `AtlasParaIsa-*` sueltas en `/datos/workspaces/personal`; al terminar un trabajo, `git worktree remove` (la rama conserva los commits).
- Comandos: `npm run typecheck`, `npm test` (201,5 s en la suite de la publicación V10, según `publication-v10.json`, que no registra la carga de la torre; con el laboratorio en marcha tarda varias veces más y alguna prueba de tiempos puede fallar, `docs/EVIDENCIA.md` del 2026-09-22. El script ya lleva `--test-timeout` de 600 s; lánzalo en segundo plano con `timeout 1200`), `npm run build`. Laboratorio: `npm run lab -- --replicas N --dias D [--param a.b=v1,v2]` con `CARTA_DATA_DIR` temporal (T016–T018, hecho).
- Hardware disponible en esta torre: 32 hilos, 125 GiB, RTX 5070 Ti + RTX 2060. Usa procesos paralelos para experimentos; la GPU solo para render/benchmark.
- NO toques `data/` del servidor público ni `~/.local/bin/atlas-servidor*`; el laboratorio usa `CARTA_DATA_DIR` temporal. El servidor público sirve `https://atlas.humanizar.tech` desde el commit desplegado (ver `docs/ESTADO.md`), no necesariamente el HEAD de `main`.
- Histórico (hasta 2026-09-23 11:17): el servidor público corría con `cwd` en ESTE árbol y servía `dist/client` en caliente (crítico 2026-09-19); hoy corre en el portátil de Steven (siguiente viñeta). Se mantiene por seguridad: **Nunca `npm run build` ni `npm run check` en el árbol principal** salvo al publicar (T043) con el servidor parado; construye en worktrees (`dist/` es local). Respaldo del mundo: `npm run respaldo` (T022) antes de cualquier publicación.
- **Publicación vigente (mundo V11 desde 2026-09-24 02:35; en la TORRE desde 2026-09-24 08:35; detalle en `docs/ESTADO.md`)**: Steven liberó el portátil y pidió una sola instancia en la torre con todos sus recursos. Servicio de usuario `atlas-publico.service` de la torre (lanzador versionado `scripts/systemd/torre/publico.sh`, código en el worktree detached `AtlasParaIsa-anexo/worktrees/publico-v11` en `5a36ce6` con su propio `node_modules` —no borrarlo—, mundo en `AtlasParaIsa-anexo/mundos/v11-20260924` con su credencial con hash `access.scrypt`, escucha en 100.64.0.1:3000, `CARTA_PARAMS=gobernador.presupuestoMs=5000`, proceso «carta-isa» para escapar de la regla de ananicy para node, drop-in CPUWeight/IOWeight 10000) y respaldo horario `atlas-respaldo-publico.timer` → `AtlasParaIsa-anexo/publicaciones/v11-respaldos`. El puente `atlas-puente.service` quedó deshabilitado. **No arrancar** ni `atlas-servidor.service`/`atlas-respaldo.timer` de la torre (V10, estado de la mudanza de tick 322 800) ni, en el portátil (`stev@100.64.0.2`, `ssh -i ~/.ssh/id_ed25519`), `atlas-publico.service`/`atlas-respaldo.timer` (deshabilitados; su copia de V11 se renombró a `~/atlas-lab/mundos/v11-20260924-MUDADO-A-TORRE-0835`): bifurcarían el mundo. Publicar en la torre: `bash scripts/deploy-check.sh <sha>` → `systemctl --user start atlas-respaldo-publico.service` y verificar la copia → `systemctl --user stop atlas-publico.service` y verificar proceso/puerto → nuevo worktree detached en `<sha>` con `node_modules` copiado y `npm run build` allí (nunca en el árbol principal), repuntar `publico.sh` → `systemctl --user start atlas-publico.service` → comprobar health, assets y estado durable por el dominio. Un mundo nuevo (reglas nuevas) va a otro directorio de `AtlasParaIsa-anexo/mundos/`, inicialmente vacío, con su `access.scrypt` (copiar el anterior conserva la contraseña sin manejarla en claro); al cambiar de mundo, repuntar también `atlas-respaldo-publico.service`. Contratos vigentes: `docs/evidencia-2026-09-24/publication-v11.json` y `publication-v11-torre.json`. Contraseña de la app: `~/.local/state/atlas-para-isa/password` en la torre (0600). Si Steven la pide directamente en el chat, se le lee y se le muestra solo en la respuesta; nunca copiarla a ficheros, commits, logs, docs ni servicios externos, no usarla para comprobar acceso y no modificar el wrapper.
- Prioridad en la torre: ananicy-cpp pone todo proceso `node` en BG_CPUIO (SCHED_IDLE, nice 16, E/S idle); el público se lanza como «carta-isa» (enlace al binario de node) para no caer en esa regla, y su drop-in le da CPUWeight/IOWeight 10000. Las réplicas del laboratorio siguen siendo `node` (SCHED_IDLE): no le quitan CPU al público.
- GitHub: el remoto es https sin credential helper; empuja con `git -c credential.helper= -c credential.helper='!gh auth git-credential' push origin <rama>`. Se publican `001-mundo-solido-masivo` (fast-forward) y `sprint/*`; `main` solo con OK de Steven y con typecheck+tests+build verdes.
- Límite de población: lo pone el HARDWARE (FR-013, ruling R17): sin topes fijos en el software; `POPULATION_HARD_LIMIT` = 1e6 es solo anticorrupción; el gobernador (`gobernador.presupuestoMs`, `src/server/governor.ts`) actúa cuando el p95 del paso supera el presupuesto. La política vigente es `techo` (`gobernador.politica`): fija como techo la población del momento del frenazo y por debajo de él solo **repone** muertes, no crece; el techo nunca baja y se retira cuando la carga vuelve a un margen sano. La política antigua `apagar` (pausar nacimientos) causó la extinción de la V7 pública. Además hoy existe un cupo global de nacimientos (`poblacion.nacimientosPorComprobacion` = 2 cada 120 pasos = 40/día, `src/world/params.ts`) que por sí solo produce la meseta ≈ 690 del público; es un regulador de software, declarado como tal en reglas 11, pendiente de volverse local tras cerrar la campaña C8 (decisión D2 del plan maestro del 23-09).
- La carta (S e I, recuerdos) solo la cambia Steven.
