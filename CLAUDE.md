# Una Carta Para Isa — guía para Claude Code

- Lee `GOAL.md`, `PLAN.md`, `docs/REGLAS.md`, `docs/EVIDENCIA.md` antes de tocar reglas. La constitución del proyecto está en `.specify/memory/constitution.md` y manda.
- Revisión 2026-09-19: `docs/REVISION-2026-09-19.md` (12 hallazgos confirmados; leerlo antes de tocar reglas).
- Flujo SDD (Spec Kit): `specs/001-mundo-solido-masivo/` quedó cerrada el 2026-09-19 (T001–T049); la feature activa es **`specs/002-mundo-ilimitado/`** (mundo que crece hasta el límite del hardware, determinista: etapas A–F, 60 tareas con modelo asignado; empezar por Gate A0 = T101/T102/T100). La 001 sigue siendo referencia: (spec, plan, research, tasks). **Para ejecutar: `/speckit-implement`.** Reparte las tareas `[P]` entre subagentes en paralelo (ficheros disjuntos) y cierra cada fase con `npm run typecheck && npm test` y un barrido de control del laboratorio. Cada tarea de `tasks.md` lleva su modelo y esfuerzo asignado; delegar vía `delegar_a_cloud` con `access=write` y `cwd` del worktree correspondiente.
- Comandos: `npm run typecheck`, `npm test` (tarda ~10 min desde la integración del 2026-09-19; `world.test.ts` solo ~5 min, `--test-timeout` 600 s ya en el script; lánzalo en segundo plano con `timeout 1200`), `npm run build`. Laboratorio: `npm run lab -- --replicas N --dias D [--param a.b=v1,v2]` con `CARTA_DATA_DIR` temporal (T016–T018, hecho).
- Hardware disponible en esta torre: 32 hilos, 125 GiB, RTX 5070 Ti + RTX 2060. Usa procesos paralelos para experimentos; la GPU solo para render/benchmark.
- NO toques `data/` del servidor público ni `~/.local/bin/atlas-servidor*`; el laboratorio usa `CARTA_DATA_DIR` temporal. El servidor público sirve `main` compilado en `https://atlas.humanizar.tech`.
- **El servidor público corre con `cwd` en ESTE árbol y sirve `dist/client` en caliente** (crítico 2026-09-19). **Nunca `npm run build` ni `npm run check` en el árbol principal** salvo al publicar (T043) con el servidor parado; construye en worktrees (`dist/` es local). Respaldo del mundo: `npm run respaldo` (T022) antes de cualquier publicación.
- Publicación (T043, hecho 3× el 2026-09-19): respaldo `sqlite3 -readonly data/world.sqlite "VACUUM INTO '<dest>'"` (el `.backup` no termina contra el escritor a 10 Hz) → `npm run build` en el árbol principal → `tmux send-keys -t atlas:0 C-c` → esperar a que muera el pid → `tmux send-keys -t atlas:0 Enter` (si «Pane is dead»: `tmux respawn-pane -k -t atlas:0 "$HOME/.local/bin/atlas-servidor-tmux"`) → `curl -H 'Host: atlas.humanizar.tech' http://100.64.0.1:3000/health`. Antes de publicar: `bash .superpowers/sdd/tasks/deploy-check.sh <sha>` (build + smoke en worktree, no toca el árbol). Comprobación pública con cookie: `curl -c jar -X POST https://atlas.humanizar.tech/api/login` (la cookie es Secure: no vale por http).
- GitHub: el remoto es https sin credential helper; empuja con `git -c credential.helper= -c credential.helper='!gh auth git-credential' push origin <rama>`. Se publican `001-mundo-solido-masivo` (fast-forward) y `sprint/*`; `main` solo con OK de Steven y con typecheck+tests+build verdes.
- Límite de población: lo pone el HARDWARE (FR-013, ruling R17): sin topes fijos en el software; `POPULATION_HARD_LIMIT` = 1e6 es solo anticorrupción; el gobernador (`gobernador.presupuestoMs`, app.ts) pausa nacimientos cuando el p95 del paso supera el presupuesto. La feature siguiente es `specs/002-mundo-ilimitado/` (paralelismo determinista CPU/GPU, estado por deltas).
- La carta (S e I, recuerdos) solo la cambia Steven.
