# Una Carta Para Isa — guía para Claude Code

- Lee `GOAL.md`, `PLAN.md`, `docs/REGLAS.md`, `docs/EVIDENCIA.md` antes de tocar reglas. La constitución del proyecto está en `.specify/memory/constitution.md` y manda.
- Estado publicado (qué commit, qué reglas, qué mundo): **`docs/ESTADO.md`**, la única fuente; README, PLAN y esta guía enlazan a ella en vez de copiarla.
- Revisiones vigentes (leerlas antes de tocar reglas): `docs/REVISION-2026-09-22.md` y `docs/REVISION-NOCHE-2026-09-22.md` (extinción pública V7, gobernador `techo`, congelación de 002). `docs/REVISION-2026-09-19.md` es histórica.
- Flujo SDD (Spec Kit): `specs/001-mundo-solido-masivo/` quedó cerrada el 2026-09-19 (T001–T049; sus casillas no se mantienen). `specs/002-mundo-ilimitado/` (mundo que crece hasta el límite del hardware, determinista) tiene la Etapa A integrada; las etapas B–F se congelaron el 2026-09-22 (`docs/REVISION-NOCHE-2026-09-22.md`) y **Steven las descongeló el 2026-09-23** para no probar solo mundos de techo bajo: la torre se reparte en ≈ 8 hilos de biología (panel fijo de 8 semillas, un brazo a la vez), ≈ 4 de mundos grandes sin techo de laboratorio y ≈ 16 + GPU de escala (PERF3, bloque E.0 y después B/C). De esa etapa A, en `tasks.md` siguen abiertas **T100** (topes de anticorrupción derivados de parámetros), **T103** y **T104** (gates de rendimiento sin cumplir) y el **Gate A (T110)**. La prioridad es la biología (criterio de 60 días de `GOAL.md`), con diagnóstico barato antes que escala. **Para ejecutar tareas SDD: `/speckit-implement`.** Reparte las tareas `[P]` entre subagentes en paralelo (ficheros disjuntos) y cierra cada fase con `npm run typecheck && npm test` y un barrido de control del laboratorio. Cada tarea de `tasks.md` lleva su modelo y esfuerzo asignado; delegar vía `delegar_a_cloud` con `access=write` y `cwd` del worktree correspondiente.
- **Orden del espacio de trabajo** (Steven, 2026-09-23): junto al repositorio solo existe `/datos/workspaces/personal/AtlasParaIsa-anexo/` con `worktrees/` (todo worktree nuevo va ahí: `git worktree add ../AtlasParaIsa-anexo/worktrees/<nombre> …`), `mundos/` (copias de mundos de la torre, incluida la V10 congelada de la mudanza), `publicaciones/` (respaldos) y `archivo/`. Nunca crear carpetas `AtlasParaIsa-*` sueltas en `/datos/workspaces/personal`; al terminar un trabajo, `git worktree remove` (la rama conserva los commits).
- Comandos: `npm run typecheck`, `npm test` (201,5 s en la suite de la publicación V10, según `publication-v10.json`, que no registra la carga de la torre; con el laboratorio en marcha tarda varias veces más y alguna prueba de tiempos puede fallar, `docs/EVIDENCIA.md` del 2026-09-22. El script ya lleva `--test-timeout` de 600 s; lánzalo en segundo plano con `timeout 1200`), `npm run build`. Laboratorio: `npm run lab -- --replicas N --dias D [--param a.b=v1,v2]` con `CARTA_DATA_DIR` temporal (T016–T018, hecho).
- Hardware disponible en esta torre: 32 hilos, 125 GiB, RTX 5070 Ti + RTX 2060. Usa procesos paralelos para experimentos; la GPU solo para render/benchmark.
- NO toques `data/` del servidor público ni `~/.local/bin/atlas-servidor*`; el laboratorio usa `CARTA_DATA_DIR` temporal. El servidor público sirve `main` compilado en `https://atlas.humanizar.tech`.
- **El servidor público corre con `cwd` en ESTE árbol y sirve `dist/client` en caliente** (crítico 2026-09-19). **Nunca `npm run build` ni `npm run check` en el árbol principal** salvo al publicar (T043) con el servidor parado; construye en worktrees (`dist/` es local). Respaldo del mundo: `npm run respaldo` (T022) antes de cualquier publicación.
- **Publicación vigente (desde 2026-09-23 11:17; detalle en `docs/ESTADO.md`): el servidor público corre en el PORTÁTIL de Steven**, no en la torre (decisión de Steven: la torre es solo laboratorio). Portátil `stev@100.64.0.2` (tailnet; LAN 192.168.78.179), acceso con `ssh -i ~/.ssh/id_ed25519`: servicio de usuario `atlas-publico.service` (lanzador `~/atlas-lab/publico.sh`, código compilado en `~/atlas-lab/servidor`, mundo V10 en `~/atlas-lab/mundos/v10-20260922`, credencial con hash `access.scrypt` en ese directorio, escucha en 100.64.0.2:3000, `CARTA_PARAMS=gobernador.presupuestoMs=5000`, drop-in de prioridad con P-cores 0-11), respaldo horario `atlas-respaldo.timer` del portátil en `~/atlas-lab/respaldos`, linger activo. El dominio sigue apuntando a la torre (100.64.0.1:3000) y el puente TCP `atlas-puente.service` de la torre lo reenvía al portátil (copias versionadas en `scripts/systemd/portatil/`). En la torre `atlas-servidor.service` y su timer de respaldo quedan **deshabilitados**: su directorio `AtlasParaIsa-anexo/mundos/v10-20260922` es el estado de la mudanza (tick 322 800) y **no debe volver a arrancarse** (bifurcaría el mundo). Publicar: `bash scripts/deploy-check.sh <sha>` en la torre → en el portátil `systemctl --user start atlas-respaldo.service` y verificar copia → `systemctl --user stop atlas-publico.service` y verificar proceso/puerto → sustituir `~/atlas-lab/servidor` por `git archive <sha>` + `node_modules` y `npm run build` allí → `systemctl --user start atlas-publico.service` → comprobar health, assets y estado durable por el dominio. Un mundo nuevo (reglas nuevas) va a otro directorio de `~/atlas-lab/mundos/`, inicialmente vacío, con su propio `access.scrypt` (`npm run access -- init` leyendo la contraseña por tubería, nunca en claro). Contratos: `docs/evidencia-2026-09-22/publication-v10.json` y los `publication-v10-pub*.json` del 23-09. Contraseña de la app: `~/.local/state/atlas-para-isa/password` en la torre (0600). Si Steven la pide directamente en el chat, se le lee y se le muestra solo en la respuesta; nunca copiarla a ficheros, commits, logs, docs ni servicios externos, no usarla para comprobar acceso y no modificar el wrapper.
- Prioridad en la torre (histórico: afectaba al público mientras corría allí): ananicy-cpp pone todo proceso `node` en BG_CPUIO (SCHED_IDLE, nice 16, E/S idle). En el portátil no hay ananicy; el drop-in `prioridad.conf` de `atlas-publico` le da CPUWeight/IOWeight 10000 y los P-cores.
- GitHub: el remoto es https sin credential helper; empuja con `git -c credential.helper= -c credential.helper='!gh auth git-credential' push origin <rama>`. Se publican `001-mundo-solido-masivo` (fast-forward) y `sprint/*`; `main` solo con OK de Steven y con typecheck+tests+build verdes.
- Límite de población: lo pone el HARDWARE (FR-013, ruling R17): sin topes fijos en el software; `POPULATION_HARD_LIMIT` = 1e6 es solo anticorrupción; el gobernador (`gobernador.presupuestoMs`, `src/server/governor.ts`) actúa cuando el p95 del paso supera el presupuesto. La política vigente es `techo` (`gobernador.politica`): fija como techo la población del momento del frenazo y por debajo de él solo **repone** muertes, no crece; el techo nunca baja y se retira cuando la carga vuelve a un margen sano. La política antigua `apagar` (pausar nacimientos) causó la extinción de la V7 pública.
- La carta (S e I, recuerdos) solo la cambia Steven.
