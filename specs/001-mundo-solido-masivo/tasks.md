# Tasks: Mundo sólido y laboratorio masivo — SPRINT HACKATHON 3 h (2026-09-19)

**Input**: `docs/REVISION-2026-09-19.md` (12 hallazgos confirmados con cifras) + `spec.md` + `plan.md` + `research.md` · **Branch**: `001-mundo-solido-masivo` · **Constitución**: manda.

**Meta del sprint** (Steven): una simulación funcional donde los habitantes **no mueran por decreto**, y donde emerjan **dinámicas cooperativas, complejas y evolutivas** medibles; presentable en escritorio y **observable en móvil**; servidor público estable. Calidad por encima de coste de tokens; velocidad por paralelismo, no por recortes.

**Reglas de ejecución** (para TODO delegado, de cualquier proveedor):
1. Eres de **un solo workstream**: toca SOLO los ficheros listados en tu tarea. Si necesitas algo de otro fichero, escríbelo en tu informe, no lo edites.
2. **Nunca** edites `src/world/params.ts` (T001 la fija; usa sus claves). Si te falta una clave, usa una constante local con comentario `// TODO params:` y repórtalo.
3. Antes de terminar: `npm run typecheck` verde y `timeout 300 npx tsx --test <tus tests>` verde. NO ejecutes `npm test` completo (tarda >2 min; lo corre la integración).
4. Determinismo: nada de `Math.random`/`Date.now`/`performance.now` en `src/world`. Usa `localRandom(seed, salt)` o `random(world)` que ya existen en `src/world/index.ts`.
5. Sin rescates ocultos (constitución I): ninguna constante que haga imposible morir; toda regla nueva tiene coste y un test que puede refutarla.
6. No toques `data/`, `~/.local/bin/atlas-servidor*`, ni la carta (S e I, recuerdos, textos de `world-shell.ts`).
7. Escribe código que lea como el circundante (denso, comentarios escasos y precisos, español en textos de usuario, inglés en identificadores como ya hace el repo).
8. Informe final: ficheros tocados, qué ley cambió y por qué, cómo se prueba, y cifras si mediste.
9. **PROHIBIDO `npm run build` / `npm run check` en el árbol principal** (`/datos/workspaces/personal/AtlasParaIsa`): el servidor público corre con `cwd` en ese árbol y sirve `dist/client` en caliente (hallazgo del crítico, 2026-09-19). Construye solo en tu worktree (`dist/` es local al worktree) o en T043 al publicar.

## Format: `[ID] [P?] [Modelo · esfuerzo] Descripción`
`[P]` = paralelizable (ficheros disjuntos) · Modelo = ruta `delegar_a_cloud` o agente Claude · Cada workstream corre en su **worktree** (`isolation: 'worktree'`) y se integra con merge.

---

## Phase 0 — Desbloqueo (0:00–0:15) · secuencial T001, luego todo en paralelo

- [ ] **T001** [Claude sonnet · high] **`src/world/params.ts`** + `tests/params.test.ts`. Interfaz `WorldParams` EXACTA (todas las claves que usarán los demás; defaults = comportamiento ACTUAL, para que T001 no cambie nada):
  ```ts
  export interface WorldParams {
    cuerpo: { longevidadBaseDias: number /*11*/; longevidadPorResiliencia: number /*4*/; longevidadPorActividad: number /*1*/; senescenciaInicioFraccion: number /*0.75*/;
              riesgoSenescenciaDiario: number /*0.02: hazard/día en maximumAge con cuerpo sano (T010)*/; riesgoSenescenciaPendiente: number /*6: pendiente Gompertz (T010)*/; cuidadoReduceRiesgo: number /*0.6: fracción de riesgo que quita salud+vitalidad plenas (T010)*/ };
    genes:  { varianzaFundadores: number /*0 = homocigotos exactos como hoy; T011 usa sd de desplazamiento alélico*/; tasaMutacion: number /*1 = multiplicador de la tasa actual*/ };
    poblacion: { maxima: number /*32*/; intervaloComprobacionTicks: number /*120*/; nacimientosPorComprobacion: number /*1*/ };
    recursos: { capacidadBosque: number /*1*/; capacidadPastizal: number /*1*/; capacidadOtros: number /*1*/; velocidadRegeneracion: number /*1 = multiplicador de 0.007/0.006*/; decaimientoFertilidad: number /*0 = sin decaimiento (hoy)*/; decaimientoComida: number /*0.0001*/ };
    persistencia: { cadaTicks: number /*1 = cada tick (hoy)*/; ventanaEventosTicks: number /*0 = sin poda (hoy)*/ };
  }
  export const DEFAULT_PARAMS: WorldParams;
  export function parseParams(input: Record<string,string> | string | undefined): WorldParams; // "cuerpo.longevidadBaseDias=14,genes.varianzaFundadores=0.15" o JSON; valida rangos; lanza con mensaje claro
  export function paramsOf(world: object): WorldParams;          // WeakMap<World,WorldParams> → DEFAULT_PARAMS si no hay
  export function setParams(world: object, params: WorldParams): void;
  export const PARAM_RANGES: Record<string, [number, number]>;  // por clave punteada
  ```
  Enchufar: `createWorld(seed, params?: WorldParams)` en `src/world/index.ts` llama `setParams`; `cloneWorld` propaga `setParams(draft, paramsOf(world))`. **No** añadir campos a `World` (evita migración de snapshot). Test: parse válido/ inválido/ rangos; `paramsOf` de un mundo nuevo === DEFAULT_PARAMS; `cloneWorld` conserva. Suite `tests/world.test.ts` sigue verde (control de que nada cambió). Ficheros: `src/world/params.ts`, `tests/params.test.ts`, `src/world/index.ts` (solo `createWorld`/`cloneWorld`).
- [ ] **T002** [P] [minimax/MiniMax-M3] Desbloquear `npm test`: (a) `package.json` `"test": "tsx --test --test-timeout=240000 tests/*.test.ts"`; (b) en `tests/render-legibility.test.ts` (L29, L49) y `tests/render-demographic-scope.test.ts` (L24-25, L104-105) mover `createServer(...)`, `listen()` y `chromium.launch()` DENTRO del `try` con `let server; let browser;` y `finally { await browser?.close(); await server?.close(); }`; (c) añadir al inicio de cada test de navegador el mismo guard que usan `tests/render.test.ts`/`render-inspector.test.ts` para saltar si no hay Chromium (`existsSync(chromium.executablePath())` → `t.skip()`). Aceptación: `timeout 120 npx tsx --test tests/render-legibility.test.ts tests/render-demographic-scope.test.ts` termina (ok o skip) en < 60 s. Ficheros: `package.json`, esos dos tests.
- [ ] **T003** [P] [orquestador] `npx playwright install chromium` (Playwright 1.63 pide `chromium-1243`; instalado `1234`). Verificar `chromium.executablePath()` existe.
- [ ] **T004** [P] [Claude sonnet · medium] Actualizar `CLAUDE.md`, `specs/.../quickstart.md` y `README.md` (sección «Laboratorio» y «Revisión 2026-09-19») para reflejar el estado real: `npm run lab` existe solo tras T016–T018; enlazar `docs/REVISION-2026-09-19.md`; documentar `npm test` (timeout, `world.test.ts` 115 s). Ficheros: `CLAUDE.md`, `specs/001-mundo-solido-masivo/quickstart.md`, `README.md`.

**Gate 0**: `npm run typecheck` ✅ · `npm test` termina ✅ · commit «params + tests desbloqueados». Desde este commit salen los worktrees de Fase 1 y 2.

---

## Phase 1 — Vida, evolución y entorno (0:15–1:15) · TODO [P] en worktrees desde Gate 0

**Objetivo científico**: la muerte debe ser **riesgo** (edad × condición × genes), no decreto; la población debe **reemplazarse** de forma continua; los genes deben **variar y heredarse** para que la selección tenga material; la tecnología debe **diversificarse**; el entorno debe ser **desigual y finito**.

- [ ] **T010** [P] [codex/gpt-5.6-sol · xhigh] **Senescencia como riesgo** (corrige C1). En `src/world/demography.ts`: sustituir el corte `if (state.age >= traits.maximumAge) {…death='senescence'}` por un **hazard** determinista: a partir de `senescenceStart`, riesgo diario `h = riesgoSenescenciaDiario · exp(pendiente · (age − maximumAge)/maximumAge)` reducido por `cuidadoReduceRiesgo · (health·vitality)` y modulado por `resilience`; la tirada usa `localRandom(seed, salt)` con salt derivado de `(personId, tick)` (determinista). La muerte por senescencia sigue registrándose con causa `senescence`; la salud baja gradualmente en la vejez (desgaste) para que sea **legible**, no súbita. `maximumAge` sigue calculándose con `paramsOf(world).cuerpo.longevidad*` (T001). Tests en `tests/demography.test.ts` (+ nuevo `tests/senescencia.test.ts`): (1) un cuerpo sano de edad = maximumAge NO muere con certeza en un tick; (2) la probabilidad acumulada de muerte a 2× maximumAge es > 0,95 para cuerpo descuidado y < 0,5 para cuerpo con salud y vitalidad plenas (medir con 2000 semillas); (3) determinismo: misma semilla → mismo resultado; (4) S e I protegidos: sin cambio. Ficheros: `src/world/demography.ts`, `src/shared/demography.ts` (si hace falta el tipo), `tests/demography.test.ts`, `tests/senescencia.test.ts`. **Aviso**: si `updateDemography` no recibe `world`, pasa `params` desde el llamador en `src/world/index.ts` (única línea permitida ahí: la llamada a `updateDemography`). **HECHOS DEL MOTOR (research.md §«Modelo de senescencia», 2026-09-19; el revisor DEBE comprobarlos)**: (a) `updateDemography` se invoca ~9 veces por persona y tick (`lineage.ts:124` real + 8 previsiones en `index.ts:228→298/301/328/349/425/483/491` y `family.ts:14`): la tirada de muerte debe ser **pura en `(personId, tick)`** (mismo resultado en las 9 llamadas) y las previsiones no deben aplicar daño real; test obligatorio: 9 llamadas en el mismo tick → misma decisión. (b) Hoy `senescenceDamage` ya deja la salud en 0,24 (A=12,5 d) / 0,10 (A=14,7 d) al llegar a `maximumAge`: el hazard **sustituye** ese corte, no se apila; el desgaste de vejez añadido no puede superar ×1,09 el actual o la salud llega a 0 antes de `maximumAge` y el decreto vuelve disfrazado. Test: un cuerpo sano a 1,2×`maximumAge` con `riesgo=0` conserva salud > 0. (c) La fertilidad termina en `senescenceStart` (`eligible()`): no la toques aquí (T031 barre `cuerpo.senescenciaInicioFraccion`). (d) Métrica de refutación: coeficiente de variación de la edad de muerte `edadMuerte.cv` = 0,081 con el corte actual; con riesgo real debe ser ≥ 0,12 — inclúyela en el test de 2000 semillas.
- [ ] **T011** [P] [gemini/pro · high] **Variación genética heredable** (corrige homogeneidad b). En `src/world/genetics.ts`: `founderGenome(traits, seed, varianza)` → cada alelo = codificación del rasgo + desplazamiento determinista `~N(0, varianza)` (Box-Muller sobre `localRandom`) recortado a [0,1], **por alelo** (heterocigosis real); `learningRate` deja de ser idéntico. En `src/world/lineage.ts`: la tasa de mutación se multiplica por `genes.tasaMutacion`. Con `varianzaFundadores = 0` el resultado es **bit a bit igual** al actual (control). Tests `tests/genetics.test.ts` (nuevo) y `tests/lineage.test.ts`: (1) varianza 0 → genoma idéntico al de hoy; (2) varianza 0,15 → los 16 fundadores tienen ≥ 12 valores distintos de `learningRate` y heterocigosis media > 0,3; (3) herencia: hijo de dos padres distintos tiene alelos de ambos; (4) determinismo. Ficheros: `src/world/genetics.ts`, `src/world/lineage.ts`, `tests/genetics.test.ts`, `tests/lineage.test.ts`, y la llamada en `src/world/index.ts:144-165` (solo pasar `paramsOf(world).genes.varianzaFundadores`).
- [ ] **T012** [P] [grok/grok-4.6 · high] **Reproducción con reemplazo y elección de pareja** (corrige homogeneidad c). En `src/world/index.ts` función `reproduce` (L906-931): (1) el tope es `paramsOf(world).poblacion.maxima` y **`MAX_POPULATION` está cableado en CINCO sitios de `assertWorld`** (research.md §«Modelo de senescencia»: L998 población, L1017 actores, L1064 bonds, L1075 members, L1079 history) — cambia los cinco a `Math.max(MAX_POPULATION, poblacion.maxima)`; con `poblacion.maxima=40` el mundo debe pasar `assertWorld` tras 10 días (test obligatorio); mantener `MAX_POPULATION` exportado como límite duro superior 128; (2) hasta `nacimientosPorComprobacion` nacimientos por comprobación si hay hueco; (3) el candidato no es «el primero en orden de inserción»: se elige entre los elegibles por **afinidad** determinista (proximidad + relación existente + no parentesco cercano vía `lineage`) con desempate por `localRandom(seed, salt(tick))`; (4) evento de crónica con causa (quiénes, dónde). Como no hay parámetro para activar la afinidad, usa una constante local `const ELECCION_POR_AFINIDAD = true` con `// TODO params:` y deja un test que compara ambos modos. Tests en `tests/family.test.ts`/`tests/demography.test.ts`: paternidad repartida (Gini del nº de hijos por padre < 0,5 tras 10 días con 32 hab.), no incesto directo, determinismo. Ficheros: `src/world/index.ts` (solo `reproduce`, `assertWorld` líneas de población), `src/world/family.ts` si hace falta, tests.
- [ ] **T013** [P] [codex/gpt-5.6-sol · high] **Entorno finito y desigual** (corrige C6 / «recursos en todos lados»). En `src/world/index.ts` `ecology()` (L183-206): capacidad de carga por bioma `K = recursos.capacidad{Bosque|Pastizal|Otros}` (bioma en `tile.biome`), crecimiento logístico `growth = velocidadRegeneracion · light · moisture · 0.007 · (1 − veg/K)` (y análogo para `food`), decaimiento de comida `decaimientoComida`. En `src/world/ecosystem-kernel.ts:94`: término `− decaimientoFertilidad · fertility` (el kernel recibe `tiles`: añade un parámetro opcional `options` a `step()` y pásalo desde `src/world/ecosystem.ts`). Con defaults (K=1, vel=1, decaimiento 0) **bit a bit igual** (control). Tests `tests/ecosystem.test.ts`, `tests/ecosystem-kernel.test.ts` (+ `tests/recursos.test.ts` nuevo): (1) defaults → estado idéntico tras 2 días; (2) con `capacidadOtros=0.2, capacidadPastizal=0.5, decaimientoFertilidad=0.001` a los 10 días la fracción de celdas con `food>0.1` está entre 30 % y 70 % y el Gini de `food` por región ≥ 0,35; (3) una celda saturada sin lluvia decae. Añadir a `src/world/statistics.ts`: `giniRecursosPorRegion`, `fraccionCeldasConComida`, `distanciaMediaAgua` (puras) expuestas en `worldStatistics`. Ficheros: `src/world/index.ts` (solo `ecology`), `src/world/ecosystem-kernel.ts`, `src/world/ecosystem.ts` (paso de options), `src/world/statistics.ts`, tests.
- [ ] **T035** [P] [Claude sonnet · high] **Agua superficial en cuencas** (SC-004 partes 2 y 3; evidencia «antes» 2026-09-19: 0 % de regiones sin agua superficial y distancia media a agua potable 3,0 celdas, objetivos ≥ 30 % y > 6). Clave nueva ya en `params.ts`: `agua.cuencas` (1 = hoy). Localiza dónde se decide `drinkingWater` de una tesela de tierra (`initializeEcosystem` en `src/world/ecosystem.ts` o `terrain.ts`) y añade un **ruido espacial de baja frecuencia determinista** (semilla + coordenadas globales, escala ~24 celdas; reutiliza el ruido/hash que ya usa `terrain.ts`) que define «cuencas»: una tesela solo conserva su agua potable (manantial/charca) si `ruido(x,y) < agua.cuencas` (con `cuencas=1` todo se conserva → **bit a bit igual a hoy**: control obligatorio). El mar (`elevation < 0.37`) no cambia. La humedad de las teselas fuera de cuenca puede quedar como está (la lluvia sigue). Determinismo: sin `Math.random`. Añade a `src/world/statistics.ts`… **NO**: `statistics.ts` lo toca T013; en su lugar exporta desde tu módulo `regionesSinAgua(tiles, tamRegion=16)` y `distanciaMediaAgua(tiles, muestras=200)` en `src/world/agua.ts` (nuevo) y el orquestador las cablea en `worldStatistics`. Tests `tests/agua.test.ts`: (1) `cuencas=1` → mundo semilla 4821 idéntico (JSON) al actual tras 1 día; (2) `cuencas=0.4` → en un mundo de 4 chunks explorados ≥ 30 % de regiones sin agua superficial y distancia media > 6; (3) determinismo entre dos mundos. Ficheros: `src/world/terrain.ts` y/o `src/world/ecosystem.ts` (solo la asignación de `drinkingWater`), `src/world/agua.ts`, `tests/agua.test.ts`.
- [ ] **T014** [P] [minimax/MiniMax-M3] **`craftTechnology` ejecuta la receta decidida** (corrige C2). En `src/world/technology.ts:462-465` y en los llamadores de `src/world/index.ts` que reciben `{kind:'craft', recipeId}` de `technologyOpportunity` (y en el comando manual de `technology-execution.ts` si aplica): transportar `recipeId` hasta `craftTechnology(host, actor, recipeId, emit)`. Mantener el fallback actual solo cuando no hay decisión. Test en `tests/technology.test.ts`: actor con `recipe-1` dominada y `recipe-2` que aporta capacidad nueva → `technologyOpportunity` elige `recipe-2` → se fabrica `recipe-2`, y el `reason` coincide. Ficheros: `src/world/technology.ts`, `src/world/technology-execution.ts`, `src/world/index.ts` (solo las 1-3 líneas de la llamada), `tests/technology.test.ts`.
- [ ] **T015** [P] [Claude sonnet · high] **Muerte con contexto legible** (completa FR-006). Donde se registra la causa de muerte (crónica en `src/world/index.ts` y/o `src/world/chronicle-journal.ts`): añadir lugar `(x,y)`, tick y los **3 eventos previos** del habitante (últimos `events` con ese actor), en el texto y en un campo estructurado `death: {cause, tick, x, y, previous: string[]}` del evento. El inspector (`src/client/inspector-view.ts`) los muestra en «Historia». Test `tests/muerte.test.ts` (nuevo): forzar una muerte por sed y comprobar el registro. Ficheros: `src/world/index.ts` (solo el bloque de registro de muerte), `src/world/chronicle-journal.ts`, `src/shared/types.ts` (campo opcional en `EventView`), `src/client/inspector-view.ts`, `tests/muerte.test.ts`.
- [ ] **T016** [P] [Claude sonnet · high] **`scripts/lab/replica.ts`**: args `--seed --dias --params "k=v,k=v" --salida <dir>`; crea `CARTA_DATA_DIR` temporal por réplica y **SIEMPRE adjunta un `Store` SQLite temporal** (P3: sin Store rigen otras leyes de tecnología) — `new Store(path)` + `store.save` cada `persistencia.cadaTicks`; `createWorld(seed, parseParams(params))`; `stepWorld` en bucle; cada día escribe `dia-NNN.json` {tick, poblacion, nacimientos, muertes por causa (del `demography` view), fundadoresVivos, generacionesVivas, diversidadOficios (entropía de `specialty()` o del oficio dominante), recetasDistintasEnUso, cooperaciones, gini/fraccionComida/distanciaAgua (de `worldStatistics` si T013 ya existe; si no `null`), p50/p95 ms por paso, rss} y al final `replica.json` {seed, params, sha (`git rev-parse HEAD`), digest de `src/world` (sha256 de ficheros ordenados), dias, resumen}. Salida determinista salvo tiempos. Test `tests/lab.test.ts` (parte 1): réplica 1 día semilla fija → claves esperadas; dos réplicas iguales → métricas idénticas. Ficheros: `scripts/lab/replica.ts`, `scripts/lab/README.md`, `tests/lab.test.ts`.
- [ ] **T017** [P] [Claude sonnet · high] **`scripts/lab/barrido.ts`**: args `--replicas N --dias D --param k=v1,v2 (repetible) --seed-base --concurrencia (default availableParallelism()-2) --timeout s --salida <dir> [--control <dir>]`; producto cartesiano; `child_process.spawn('npx',['tsx','scripts/lab/replica.ts',…])` con cola; mata a los `--timeout` y marca `abortada`; `progreso.log` con % y ETA; al final invoca `resumen.ts`. Añadir a `package.json`: `"lab": "tsx scripts/lab/barrido.ts"`, `"lab:replica": "tsx scripts/lab/replica.ts"`, `"lab:resumen": "tsx scripts/lab/resumen.ts"`. Test `tests/lab-barrido.test.ts` (nuevo, para no chocar con T016): barrido 3 réplicas × 1 día `--concurrencia 2` termina y encola. Ficheros: `scripts/lab/barrido.ts`, `package.json` (solo scripts), `tests/lab-barrido.test.ts`.
- [ ] **T018** [P] [Claude sonnet · high] **`scripts/lab/resumen.ts`**: lee un directorio de barrido; por grupo de parámetros agrega mediana/p10/p90 de: supervivencia de fundadores al día D, población final/inicial, muertes por causa, diversidad, gini, fracción de celdas con comida, distancia a agua, p95 ms; compara con `--control` (delta y semáforo 🟢🟡🔴 con umbrales SC-002..005 de `spec.md`); detecta rotura de determinismo (misma semilla + mismo digest ≠ métricas → 🔴); escribe `resumen.json` y `resumen.md` (tabla). Test `tests/lab-resumen.test.ts` con fixture de 2 réplicas sintéticas. Ficheros: `scripts/lab/resumen.ts`, `tests/lab-resumen.test.ts`.

---

## Phase 2 — Servidor estable y móvil observable (0:15–1:15) · [P] en paralelo con Fase 1

- [ ] **T019** [P] [Claude sonnet · high] **Índice de diversidad de conducta** (FR-007, SC-003; research.md decisión 3). `src/world/diversidad.ts` (nuevo): `vectorConducta(person, world): number[]` construido SOLO con datos que el mundo ya registra por persona (oficio dominante/`specialty`, competencias y objetos de `person.technology`, tipos de alimento y lugares en sus memorias/eventos recientes con ese actor, acciones recientes) — **sin añadir campos a `World` ni a la persona** (no romper `assertWorld` ni el snapshot); vector normalizado L2 de dimensión fija documentada. `indiceDiversidad(world): {conducta: number, oficios: number, total: number}` = distancia coseno media entre todos los pares de habitantes vivos (0 = clones, 1 = ortogonales) y entropía normalizada de oficios dominantes; `total` = media de ambas. Determinista, O(n²) con n ≤ 128 (≤ 8.128 pares: barato). **No edites `statistics.ts`** (lo toca T013; el orquestador cablea `indiceDiversidad` en `worldStatistics` al integrar). Tests `tests/diversidad.test.ts`: dos clones → conducta 0; dos personas con oficios y memorias opuestas → > 0,8; mundo nuevo semilla 4821 tras 2 días → valor estable y determinista (mismo número en dos ejecuciones); 0 ó 1 habitante → 0 sin excepción. Ficheros: `src/world/diversidad.ts`, `tests/diversidad.test.ts`.
- [ ] **T020** [P] [Claude opus · high] **Acotar `state`** (corrige C4). `projectTechnology` (`src/world/technology.ts:550`) devuelve `recipes` como **resumen** `{id, name, capacities, generation}` (sin `program`), y el detalle de una receta se pide con mensaje `{type:'recipe', id}` → respuesta `{type:'recipe', recipe}` (tipar en `src/shared/types.ts`: `ClientMessage` **nuevo tipo compartido** para todos los mensajes de subida, y `ServerMessage` ampliado); `events` y `memories` en `projectWorld` limitados a los últimos 200/100 (respetar `MAX_EVENTS` si ya acota). Cliente: donde se dibujan recetas (`src/client/technology-art.ts`, `inspector-view.ts`, `ui-catalog.ts`) usar el resumen y pedir detalle al abrir. Declarar `instanceId` en `WorldView` (P4). Medir antes/después con `JSON.stringify(state).length` en un test (`tests/world-view-size.test.ts`, nuevo): a t=8000 con 32 hab. medir y dejar constancia. **Aceptación real de T020 (medida 2026-09-19): `state` 595,2 KiB (−24,7 % desde 790,6 KiB); techo provisional de esta fase `< 640 KiB`.** El `< 120 KiB` del brief **NO es alcanzable dentro de esta tarea** y deja de ser su criterio: con las recetas en resumen, `tiles` pesa 291,8 KiB y `people` 82,7 KiB, así que borrar `technology` entero dejaría ~487 KiB. El objetivo `< 120 KiB` se re-escala a la tarea de **delta / página sucia de `tiles`** (causa (a) del crítico C4), fuera de los ficheros de T020. Ficheros: `src/world/index.ts` (solo `projectWorld`), `src/world/technology.ts` (solo `projectTechnology`), `src/shared/types.ts`, `src/server/app.ts` (handler del mensaje `recipe`), `src/client/{connection,technology-art,inspector-view,ui-catalog}.ts`, test.
- [ ] **T021** [P] [Claude opus · high] **Guardado por cadencia + poda** (corrige C3, C5, C10, C12). En `src/server/app.ts` `stepOnce`: `store.save` solo si `valid.length > 0 || world.tick % paramsOf(world).persistencia.cadaTicks === 0` (producción: `main.ts` pasa `cadaTicks: 20` = 2 s vía `createWorld`/`setParams` al cargar; los tests que asumen guardado por tick siguen con default 1); mantener `cloneWorld` (los gestos lo necesitan) y `runtime.stepMs` como hoy. En `src/server/store.ts`: `PRAGMA synchronous=NORMAL` (WAL ya activo; documentar: pérdida ≤ último commit ante corte de luz, no corrupción); **poda** en `save()` según `ventanaEventosTicks` (0 = sin poda): eventos con `tick < world.tick − ventana` y, en `chunks`, todas las versiones salvo la más reciente por `key`; ajustar `assertChronicleArchive` (compara `eventCounter`) a la ventana. `Store.load()` envuelto en `BEGIN…COMMIT` (C10). Tests `tests/server.test.ts`, `tests/store-*.test.ts`, `tests/chronicle-store.test.ts`: cadencia 20 → un gesto fuerza guardado inmediato; poda mantiene la última versión de cada chunk y los eventos de la ventana; `load` bajo escritura concurrente no falla (dos `Store` sobre el mismo fichero, uno guardando). Medir p95 del paso con 32 hab. dispersos antes/después con `scripts/benchmark-simulation.ts` y anotar en el informe. **Añadido (crítico 2026-09-19)**: (a) `save()` valida el mundo con `assertWorld` ANTES de escribir (con cadencia 20 el coste es asumible) y rechaza guardar un estado que `load()` rechazaría (hoy `save()` acepta lo que `load()` rechaza: reproducido); (b) tercer slot de respaldo: cada 100 guardados copiar slot 0 → slot 2 (profundidad de recuperación de minutos, no de 100 ms; `slot` es dato, no esquema); (c) en `stepOnce` medir el ritmo real: `runtime.tickHz` (ticks por segundo de reloj de pared en ventana de 120) y exponerlo en `performance` del `state`; sustituir `setInterval` por `setTimeout` re-planificado con compensación de deriva (`next += tickMs`). Ficheros: `src/server/app.ts` (solo `stepOnce` y el planificador), `src/server/store.ts`, `src/server/main.ts`, `src/shared/types.ts` (campo `tickHz` en `performance`), tests.
- [ ] **T022** [P] [Claude sonnet · medium] **Respaldo automático del mundo de Isa** (hueco del crítico: 6,1 GiB/día, sin respaldo). `scripts/respaldo.sh`: `sqlite3 -readonly "$DB" "VACUUM INTO '$DEST/world-$(date +%Y%m%d-%H%M).sqlite'"` (**NO `.backup`**: la API de backup se reinicia cada vez que otro proceso escribe la fuente y con el servidor a 10 Hz nunca termina — comprobado 2026-09-19 10:23, timeout 600 s; `VACUUM INTO` con `-readonly` hace una instantánea consistente en una sola transacción de lectura WAL: 479 MB en < 1 s, `quick_check ok`; **solo lectura sobre `data/`**, sin checkpoint al cerrar), gzip, retención 48 copias, `DEST` por defecto `~/.local/state/atlas-para-isa/backups/`; verificación `PRAGMA quick_check` de la copia. Unidad de usuario systemd `~/.config/systemd/user/atlas-respaldo.{service,timer}` cada hora (`OnCalendar=hourly`, `Persistent=true`) — **crear los ficheros y documentar el `systemctl --user enable --now`; NO activarlo: lo activa Steven en T043**. Test `tests/respaldo.test.ts`: sobre una BD temporal creada con `Store`, el script produce una copia que `Store` abre y `load()` acepta. Ficheros: `scripts/respaldo.sh`, `scripts/systemd/atlas-respaldo.service`, `scripts/systemd/atlas-respaldo.timer`, `tests/respaldo.test.ts`, `package.json` (script `"respaldo": "bash scripts/respaldo.sh"`).
- [ ] **T023** [P] [minimax/MiniMax-M3] **Limitador de login tras proxy** (corrige C7). `src/server/app.ts:189`: clave = si `req.socket.remoteAddress` es loopback (`127.0.0.1`, `::1`, `::ffff:127.0.0.1`) y hay `X-Forwarded-For`, usar su **primer** valor validado como IP (`net.isIP`), si no la IP de socket; añadir un segundo cupo global (`rate(loginGlobal,'*',60,60_000)`). Test en `tests/server.test.ts` (añadir al final): dos XFF distintos desde loopback no comparten cupo; XFF desde no-loopback se ignora. Ficheros: `src/server/app.ts` (solo `rate`/login), `tests/server.test.ts`.
- [ ] **T024** [P] [Claude sonnet · high] **Modo ligero móvil** (corrige P1; FR-008). `src/client/modo.ts` (nuevo): `decidirModo(): 'completo'|'observador'` por `?modo=`, `localStorage`, `matchMedia('(max-width:700px)')`, `navigator.deviceMemory < 4`, sin WebGL2 → observador; conmutador visible en la barra. `src/client/landscape.ts`: `dpr = min(devicePixelRatio, modo==='observador' ? 1 : 1.5)`; en observador el bucle rAF corre a 30 fps y no interpola. `src/client/game.ts`: en observador no instanciar `GpuTerrain` (Canvas 2D directo) y enviar `{type:'suscripcion', intervaloMs: 5000}` (si T020 aún no define `ClientMessage`, usa el literal y anótalo) — el servidor (`app.ts`) respeta el intervalo por cliente (mínimo 1000) en `broadcast`. Test `tests/modo.test.ts` (tabla de casos) y `tests/connection.test.ts` (suscripción). Capturas antes/después con un driver Playwright (base: `scripts/smoke.ts`) → `artifacts/movil-antes.png`, `artifacts/movil-despues.png`. Ficheros: `src/client/modo.ts`, `src/client/landscape.ts` (solo `resize`/`frame`), `src/client/game.ts` (arranque y barra), `src/client/world-shell.ts` (botón conmutador), `src/server/app.ts` (handler `suscripcion` + intervalo por cliente — T020/T023/T024 añaden cada uno su `case`/bloque; merge por orden), tests.
- [ ] **T033** [P] [Claude sonnet · high] **Mapa de calor de recursos** (FR-010; US3 escenario 3). `src/client/calor.ts` (nuevo, módulo PURO sin DOM global): `capasDeCalor = ['comida','vegetacion','agua','fertilidad','madera'] as const`; `colorCalor(capa, valor: number): string` (rampa perceptual 5 paradas, valor 0..1); `dibujarCalor(ctx: CanvasRenderingContext2D, tiles: readonly TileView[], capa, view: {x0,y0,tileSize,scale}, alpha=0.55)` que pinta un rectángulo por tesela visible; `leyendaCalor(capa): {titulo: string, paradas: {valor:number,color:string,etiqueta:string}[]}`; `totalesPorRegion(tiles, capa, tamRegion=16): Map<string, number>` para comparar con `worldStatistics` (US3: los totales del cliente deben coincidir con el servidor). Lee `src/shared/types.ts` para el tipo real de tesela que llega al cliente (`TileView`/`WorldView.tiles`). **No edites `landscape.ts`, `game.ts` ni `world-shell.ts`** (los tiene T024): el orquestador cablea la tecla `H` y la leyenda al integrar. Tests `tests/calor.test.ts` con un canvas falso (objeto con `fillRect` que registra llamadas): una tesela por celda visible, colores monótonos con el valor, `totalesPorRegion` suma exactamente `food` por región de 16×16. Ficheros: `src/client/calor.ts`, `tests/calor.test.ts`.
- [ ] **T034** [P] [Claude sonnet · high] **Pase visual: luz diurna y agua viva** (FR-010; US4 «espectacular»). `src/client/luz.ts` (nuevo, módulo PURO): `tinteDeFase(phase: 'dawn'|'day'|'dusk'|'night', tick: number): {r,g,b,a}` (interpolación suave dentro de cada fase usando `TICKS_PER_DAY` de `src/world/index.ts` y `phaseAt`; amanecer cálido, día neutro, atardecer naranja, noche azul profunda con alpha ≤ 0,45 para no tapar la escena); `sombraLarga(phase, tick): {dx, dy, alpha}` para las sombras de árboles/personas; `brilloAgua(tick, x, y, weather: 'clear'|'rain'): number` (0..1, seno de baja frecuencia con desfase por celda, determinista; con lluvia ondas más cortas); `humoDeHogar(tick, seed): {dx, dy, alpha}[]` (3–5 partículas deterministas). Todo puro, sin DOM, sin `Math.random`. **No edites `landscape.ts`** (T024); el orquestador lo cablea. Tests `tests/luz.test.ts`: continuidad (sin saltos > 0,05 entre ticks consecutivos), rango de alpha, determinismo, `brilloAgua` periódico. Ficheros: `src/client/luz.ts`, `tests/luz.test.ts`.
- [ ] **T025** [P] [Claude sonnet · medium] **`syncFauna` incremental** (P2). `src/world/animals.ts:78-91,311`: mantener un `Set` de tiles con fauna en el tick anterior y recorrer solo `anterior ∪ actual`. Determinismo intacto (orden canónico). Test en `tests/animals.test.ts`: estado idéntico a la versión completa tras 500 ticks. Ficheros: `src/world/animals.ts`, `tests/animals.test.ts`.

**Gate 1+2** (integración, orquestador): merge de los worktrees en orden T014, T015, T025, T023, T011, T013, T012, T010, T016, T017, T018, T020, T021, T024 (de menos a más conflictivo) → `npm run typecheck && npm test && npm run build` ✅ → **revisión adversarial del diff** (T041) → commit «vida, evolución, entorno, servidor, móvil».

---

## Phase 3 — Evidencia (0:50–1:45) · en segundo plano en cuanto T016–T018 estén verdes

- [ ] **T030** [orquestador] **Línea base** con las reglas VIEJAS: `git worktree add /tmp/base-lab <commit Gate 0>`; copiar `scripts/lab` y los scripts de `package.json`; `npm run lab -- --replicas 32 --dias 10 --seed-base 1000 --salida artifacts/lab/base-<sha>`. Anotar en `docs/EVIDENCIA.md` §«2026-09-19 · Línea base» (supervivencia, causas, diversidad, gini, fracción de comida, p95). **Estas cifras fijan SC-002..005 definitivos.**
- [ ] **T031** [orquestador + gemini/pro para leer `resumen.md`] **Barrido de calibración** con las reglas NUEVAS (tras Gate 1+2): **rejilla derivada analíticamente en research.md §«Modelo de senescencia» (no barrer a ciegas)**: `--param cuerpo.riesgoSenescenciaDiario=0.04,0.08 cuerpo.riesgoSenescenciaPendiente=6,10 cuerpo.cuidadoReduceRiesgo=0.6,0.8 cuerpo.senescenciaInicioFraccion=0.75,0.9 poblacion.maxima=40 poblacion.nacimientosPorComprobacion=2 --replicas 8 --dias 25 --control artifacts/lab/base-<sha>` (16 combinaciones × 8 = 128 mundos de 25 días; 30 procesos ≈ 15 min) y, en paralelo, el barrido de entorno `--param recursos.capacidadOtros=0.2,0.35 recursos.capacidadPastizal=0.5,0.7 recursos.decaimientoFertilidad=0.0005,0.001 genes.varianzaFundadores=0.1,0.15,0.25 --replicas 8 --dias 10`. Predicción del modelo: fundadores vivos a día 10 entre 14,5 y 16 de 16 (S e I son inmortales: solo 14 pueden morir), 0 extinciones a 25 días, 6,5–8 generaciones vivas. **Métrica de refutación**: `edadMuerte.cv` ≤ 0,10 → 🔴 «decreto disfrazado». Fallback sin barrido: `riesgo=0.04, pendiente=10, cuidado=0.6, maxima=40, nacimientos=2`. Si pop25 > 32 sistemáticamente, bajar `maxima` a 36 antes que tocar el hazard. Elegir defaults que cumplan **SC-002 (supervivencia ≥ 70 %), SC-003 (diversidad ≥ 0,6), SC-004 (Gini ≥ 0,35) sin población > 200 %**; escribirlos en `DEFAULT_PARAMS` (**única edición permitida de `params.ts` tras T001**, la hace el orquestador) y en `docs/REGLAS.md`. Fila en EVIDENCIA con SHA, semillas, cifras.
- [ ] **T032** [orquestador] **Barrido largo** 16 × 25 días con los defaults finales, en segundo plano durante la Fase 4; fila en EVIDENCIA (recambio: ≥ 3 generaciones vivas al día 25, ninguna extinción).

---

## Phase 4 — Cierre y publicación (1:45–2:30) · buffer 0:30

- [ ] **T036** [Claude opus · high] **Integración de cableado (post-merge, árbol principal, tras Gate 1+2)** — todo lo que los workstreams no podían tocar por alcance: (a) `src/client/game.ts`: en modo observador enviar `{type:'suscripcion', intervaloMs: 5000}` al conectar (tipo `ClientMessage` de T020) y **no** instanciar `GpuTerrain` (constructor de `Landscape` condicionado a `modo`); (b) `deathHistory` (T015, `inspector-view.ts`) mostrada en la pestaña «Historia» del inspector; (c) tecla `H` + leyenda del **mapa de calor** (T033 `calor.ts`) en `landscape.ts`/`game.ts`; (d) **luz y agua** (T034 `luz.ts`): `tinteDeFase` como capa sobre la escena, `brilloAgua` en las teselas de agua, `sombraLarga` en árboles/personas, `humoDeHogar` en asentamientos — con `prefers-reduced-motion` y modo observador respetados; (e) `worldStatistics` (`statistics.ts`): añadir `diversidad` (T019 `indiceDiversidad`), `regionesSinAgua` y `distanciaMediaAgua` (T035 `agua.ts`) junto a las de T013; (f) `PROTOCOL_VERSION` 6 → 7 en `src/shared/types.ts` (T020 cambió el cable) y `assertWorld`/migración si aplica; (g) invalidar la caché `recipeDetails` del cliente al cambiar de tick (T020); (h) **dieta del `state` guiada por medición** (`docs/evidencia-2026-09-19/state-desglose-t020.md`: cámara 12×8 = 311 KiB; 40×28 = 595 KiB): redondear los floats de las teselas a 3 decimales en `projectTerrain` (267 B/tesela → ~120), sacar `experiences` y `trust` de `PersonView` y servirlos bajo demanda con `{type:'persona', id}` (igual que `recipe`), en `technology` enviar solo recetas/items/knowledge referenciados por habitantes vivos, `events` → últimos 40 en el `state` (el resto por `{type:'cronica'}`), `organization` como resumen (comunidades y totales, sin listas por persona). Objetivo medible: **< 120 KiB con cámara 12×8 y < 250 KiB con 40×28** a t=8000 con 32 habitantes (`tests/world-view-size.test.ts` con esos dos asserts reales). Verificación: `npm run typecheck`, `npm test` completo (en worktree o con el servidor público parado: **nunca `npm run build` en el árbol principal**), driver de capturas escritorio+móvil, y `state` medido < 120 KiB. Ficheros: `src/client/{game,landscape,inspector-view}.ts`, `src/world/statistics.ts`, `src/shared/types.ts`, tests afectados.
- [ ] **T040** [orquestador] `npm run check` ✅; e2e `npm run test:e2e` (con Chromium de T003); levantar local con `CARTA_DATA_DIR` temporal y pasar el driver (escritorio + móvil) → capturas `artifacts/final-escritorio.png`, `artifacts/final-movil.png`; comprobar en la UI: día ≥ 15 sin extinción, censo con ≥ 2 generaciones, crónica con muertes explicadas.
- [ ] **T041** [P] [Claude opus ×3 lentes: determinismo · conservación de materia · regresión de supervivencia] **Revisión adversarial del diff completo** Gate 0 → HEAD (workflow); cada hallazgo CONFIRMADO se arregla antes del merge (delegar al modelo del workstream original).
- [ ] **T042** [P] [gemini/flash · high] Docs: `docs/REGLAS.md` (sección «Parámetros» con tabla de `WorldParams`, rangos y defaults elegidos, y las leyes nuevas: senescencia por riesgo, capacidad de carga, elección de pareja), `README.md` (Laboratorio), `PLAN.md` (estado 2026-09-19), `docs/EVIDENCIA.md` (consolidar T030–T032). Ficheros: solo docs.
- [ ] **T043** [orquestador, con Steven] Merge `001-mundo-solido-masivo` → `main`; push; `npm run build`; relanzar el servidor público (ventana 0 del tmux `atlas`: Ctrl-C, Enter); verificar `https://atlas.humanizar.tech` en escritorio y móvil real; **mundo nuevo por versión publicada** (constitución): renombrar `data/world.sqlite` a `data/world-<fecha>.sqlite` antes de relanzar (Steven decide). **Checklist de publicación (crítico 2026-09-19)**: (1) `npm run respaldo` ANTES de tocar nada; (2) activar el timer de T022 (`systemctl --user enable --now atlas-respaldo.timer`); (3) crear `data/access.scrypt` con `npm run access -- init` y quitar `export CARTA_PASSWORD=` de `~/.local/bin/atlas-servidor` (hoy la contraseña está en claro en `/proc/<pid>/environ`); (4) `npm run build` SOLO en este paso, con el servidor parado (sirve `dist/` en caliente); (5) tras relanzar, comprobar `performance.tickHz ≈ 10` y `p95StepMs < 50` en el `state`; (6) **`export CARTA_PROXY_IP=<IP de malla del VPS con Caddy>`** en `~/.local/bin/atlas-servidor` (T023: sin esa variable el limitador por `X-Forwarded-For` queda inactivo; Caddy sí envía la cabecera) y probar dos IPs distintas → cupos separados.

---

## Pospuesto explícitamente (fuera del sprint, con motivo)
- **US5 refactor** de `index.ts`/`landscape.ts` (T050–T053 antiguos): la arquitectura de capas es sana (revisión §3); partir 3.200 líneas en 3 h con 14 workstreams tocando `index.ts` en paralelo garantiza conflictos. Se hace después del evento con el laboratorio como control.
- **Mapa de calor** (T033 antiguo) y **Lighthouse formal** (T044 antiguo): solo si sobra buffer; el driver de capturas ya mide lo esencial.
- `agua.cuencas`, `social.imitacion`: sin hallazgo que los respalde hoy; se añaden cuando el laboratorio los pida.

## Dependencies
T001 → (todo lo demás). T016 ∥ T017 ∥ T018 → T030 → T031 → T032. Gate 1+2 → T031. T041 antes de T043. `src/server/app.ts` lo tocan T020/T021/T023/T024 en hunks distintos (handlers `case`, `stepOnce`, `rate`): merge por orden indicado en Gate 1+2. `src/world/index.ts` lo tocan T001/T011/T012/T013/T014/T015/T020 en funciones distintas (`createWorld`/`cloneWorld`, fundadores, `reproduce`, `ecology`, llamada craft, registro de muerte, `projectWorld`).

## Modelos por tarea (resumen; motivo en `plan.md`)
| Tarea | Modelo | Esfuerzo | Por qué |
|---|---|---|---|
| T010 senescencia, T013 entorno | codex/gpt-5.6-sol | xhigh / high | Los dos modelos científicos delicados (hazard, logística): el más capaz |
| T011 genética | gemini/pro | high | Razonamiento + lectura de genetics/lineage/index en contexto largo |
| T012 reproducción | grok/grok-4.6 | high | Lógica no trivial en fichero grande; reparte carga entre proveedores |
| T014, T023, T002 | minimax/MiniMax-M3 | — | Cambios pequeños y bien especificados |
| T015, T016, T017, T018, T024, T025, T001, T004 | Claude sonnet | high | Codificación seria, cuota Claude holgada |
| T020, T021 | Claude opus | high | Contrato compartido y transacciones SQLite: lo más arriesgado |
| T041 | Claude opus ×3 | high | Verificación adversarial (rúbrica 4) |
| T042 | gemini/flash | high | Documentación masiva |
| Orquestación, gates, T030–T032, T043 | Fable 5.1 (orquestador) | — | Decisiones críticas y síntesis no se delegan |
